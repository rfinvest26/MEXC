# Render Blueprint for MEXC Web App + API Server
services:
  - type: web
    name: sellbitweb-mexc
    runtime: node

    rootDir: .

    buildCommand: npm ci && npm run build && npm run build:server
    startCommand: npm run start:server

    autoDeployTrigger: commit
